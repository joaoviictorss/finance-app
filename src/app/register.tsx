import { SafeAreaView, Text, View } from "react-native";

const RegisterPage = () => {
  return (
    <SafeAreaView>
      <Text>Register page</Text>
    </SafeAreaView>
  );
};

export default RegisterPage;
