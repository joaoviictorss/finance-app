import { SafeAreaView, Text } from "react-native";

const Transactions = () => {
  return (
    <SafeAreaView>
      <Text>Transactions</Text>
    </SafeAreaView>
  );
};

export default Transactions;
